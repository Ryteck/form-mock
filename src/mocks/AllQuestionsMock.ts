export const QuestionsMock = [
  'Não consigo acessar o site.',
  'Não sei o link para acessar o site.',
  'Problema genérico.'
].map(item => ({label: item, value: item}))

export const SubQuestionMock = [
  'Usuário não existe.',
  'Senha incorreta.',
  'Acesso negado.'
].map(item => ({label: item, value: item}))